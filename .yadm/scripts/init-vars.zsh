# Git credentials
GIT_AUTHOR_NAME="Rob Wise"
GIT_COMMITTER_NAME="$GIT_AUTHOR_NAME"
git config --global user.name "$GIT_AUTHOR_NAME"
GIT_AUTHOR_EMAIL="robert.wise@outlook.com"
GIT_COMMITTER_EMAIL="$GIT_AUTHOR_EMAIL"
git config --global user.email "$GIT_AUTHOR_EMAIL"

# Rails Mailer Testing
GMAIL_USERNAME="robwiserailsapptester@gmail.com"
GMAIL_PASSWORD="passwordfoobar"
RAILS_ADMIN_PASSWORD="passwordfoobar"
